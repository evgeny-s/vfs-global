# VFS Global checker for visa dates availability

### How to use:
1. Install packages with command:
```shell
nvm exec npm i
```

2. Update `.env` file

3. Execute the command:
```shell
nvm exec npm start
```